# 밴드 합주 일정 수합 프로그램 — 프로젝트 브리프

## 문제 정의
- 밴드 멤버 4~7명, 매주 스케줄이 다름 (요일/시간 고정 불가, 산발적)
- 기존 방식: 스프레드시트에 한 달치 날짜별 오전/오후/새벽 체크 → 시간대 구분이 모호하고 매번 수정해야 해서 번거로움
- 기존 일정 조율 도구들은 보통 주 단위만 지원하는데, 한 달을 잡아도 전원 가능한 슬롯이 1~2개뿐이라 주 단위 범위로는 일정을 못 잡을 확률이 높음 → 자유로운 날짜 범위 + 세분화된 시간 단위 + 반복 패턴 기반 프로그램이 필요

## 핵심 데이터 구조
- **User (개인 계정)**: 기본 패턴과 예외를 개인 전역으로 소유 (여러 밴드에 자동 반영됨). 여러 Team에 소속 가능
- **Team (밴드)**: 멤버 목록, 리더
- **TeamMember**: User ↔ Team 다대다 매핑
- **확정된 합주 일정**: Team에 속하지만, 확정되는 순간 관련 멤버들의 개인 예외에도 자동 반영 → 다른 밴드 일정을 잡을 때 그 시간이 자동으로 "불가" 처리됨

## 상태값
가능 / 불가능 / 미정 — 3단계. 그리드 셀을 탭하면 가능 → 불가능 → 미정 순으로 순환

## 입력 방식
- **기본 패턴**: 요일 × 시간 반복 그리드 (에브리타임 스타일, 한 주 템플릿)
- **예외**: 캘린더에서 날짜 선택 → 그 날짜의 그리드가 기본 패턴 값으로 미리 채워진 채로 열림 → 같은 토글 방식으로 그날만 수정 (기존 가능을 불가/미정으로 바꾸는 것도, 기존 불가를 가능으로 추가하는 것도 둘 다 가능) → "기본 패턴으로 되돌리기"로 예외 제거
- 실제 그리드 시간 단위: 30분

## 교집합 계산 로직
- 슬롯(시간대)마다 가능 인원 수 계산
- 연속 슬롯을 이어붙여 "연속 가능 블록" 탐색, 최소 연속 시간(예: 2~3시간) 미만은 후보에서 제외
- 기본: 전원 가능 블록 우선 표시
- 옵션: N-1명 가능 랭킹으로 대체 후보 표시, 또는 원하는 최소 인원 수(N명 이상)를 직접 지정해서 필터링

## 권한
- 리더: 팀 생성, 멤버 초대 승인, 수합 기간/마감일 설정, 슬롯 확정
- 멤버: 자기 가능 시간 입력만

## 결과 표시
- 달력 히트맵 + 리스트 뷰 둘 다 제공
- 날짜 클릭 시 시간대별 상세 (누가 가능/불가능/미정인지) 확인
- 슬롯 확정 시 팀 일정으로 등록

## 화면 구조 (IA)
1. 로그인 (카카오 로그인)
2. 팀 선택/생성 (내 밴드 목록, 팀 만들기 / 초대코드로 참여)
3. 내 스케줄 관리 (기본 패턴 / 예외 탭)
4. 팀 대시보드 (달력 히트맵 + 리스트, 수합 현황·마감일)
5. 날짜 상세 (시간대별 멤버 가능여부, 슬롯 확정)

## 기술 스택
- **Next.js** (프론트+백엔드 한 프로젝트) + **Vercel** 배포 + **Supabase**(Postgres DB)
- 로그인: **카카오 소셜로그인** — 표준 OAuth 인가 코드 방식으로 직접 구현 (프론트 로그인 버튼 → 인가 코드 → Next.js API Route에서 토큰 교환 → 유저 정보 DB 저장 → 세션 발급)
- 소규모 프로젝트라 카카오 디벨로퍼스에서 "개인 개발자 비즈 앱 전환 + 테스트 앱" 상태로 두면 정식 심사 없이 이메일 동의항목까지 사용 가능. 단, 디벨로퍼스 콘솔에 팀원으로 등록된 사람만 로그인 가능하므로 밴드 멤버들을 팀원으로 등록해둘 것

## 부가 기능 (일정 수합 외 — 밴드 운영 지원)

### 1. 곡 후보 올리기 / 투표
- `Song` 테이블: 제목, 아티스트, 레퍼런스 링크(유튜브 등), 올린 사람, 상태(후보/연습중/셋리스트 확정/보류)
- `Vote`: User ↔ Song 다대다. 소규모 인원이므로 "찬성 인원 수" 카운트 방식 권장 (점수제보다 단순)
- 후보 목록 화면: 득표순 정렬, 각자 몇 곡 올렸는지 표시(편중 방지)

### 2. 연습해와야 하는 곡 (개인 숙제 트래킹)
- `PracticeAssignment`: Song ↔ Team ↔ (선택) 특정 합주 일정에 연결. "이번 합주까지 연습해올 곡" 지정
- 멤버별 상태는 단일 완료 체크보다 자유 코멘트 방식 권장 (파트마다 진행 상황이 다름 — 예: "1절까지 완성", "코드는 외웠는데 속도가 안 나옴")
- 합주 일정 확정 화면에 그 일정에 연결된 연습곡 목록이 같이 노출되는 구조

### 3. 셋리스트 메모
- `Setlist`: 이름(예: "9월 정기공연"), 곡 순서(Song 배열), 곡별 메모(키 변경, 전환 큐, MR 유무 등)
- 드래그로 순서 변경
- 곡별 예상 소요시간 입력 시 총 러닝타임 자동 합산

### 모듈 간 상태 흐름
Song은 후보 → 연습 중 → 셋리스트 확정 순으로 상태값이 흘러가는 파이프라인 구조:
1. 후보 등록 → 투표로 득표순 정렬
2. 리더가 "연습곡"으로 지정 → PracticeAssignment 생성, 특정 합주 일정에 연결
3. 충분히 다듬어지면 Setlist에 편입 → 순서/메모 부여

화면은 후보함 / 연습 중 / 셋리스트 3개 탭 구조로 자연스럽게 나뉨. DB 설계 시 Team/합주 일정 관련 테이블과 함께 이 3개 테이블(Song, Vote, PracticeAssignment, Setlist)도 같이 마이그레이션에 포함할 것.

## 알림 (MVP 이후 2차 개발)
- 웹 푸시(Web Push) 방식으로 결정 — 카카오 알림톡은 사업자 등록·비즈니스 채널·발송대행사 계약이 필요해서 제외
- iOS는 홈 화면에 추가(PWA 설치)하지 않으면 푸시 자체가 안 오므로 온보딩에서 안내 필요
- 핵심 로직(슬롯 확정 등)과 분리된 부가 기능이라 MVP엔 넣지 않고 나중에 이벤트 발생 지점에 호출만 끼워 넣으면 됨

## UI 컴포넌트 (React, Tailwind 기반) — 설계 완료, 이 환경에서 재작성 필요
- `ScheduleGrid` — 3단계 토글 그리드 (기본 패턴/예외 공용 재사용 컴포넌트)
- `MiniCalendar` — 예외 날짜 선택용 월 캘린더 (예외 있는 날짜 표시)
- `MySchedulePage` — 기본 패턴/예외 탭을 결합한 "내 스케줄" 화면. 시간 단위는 30분으로 설정

## 다음 단계
1. Next.js 프로젝트 초기화 + Tailwind 설정, 위 3개 컴포넌트 구현 (TIMES를 30분 단위로)
2. Supabase 스키마 설계 (User, Team, TeamMember, BasePattern, Exception, ConfirmedSchedule, Song, Vote, PracticeAssignment, Setlist)
3. 카카오 로그인 OAuth 플로우 구현
4. 교집합 계산 로직 구현
5. 팀 대시보드 / 날짜 상세 / 슬롯 확정 화면 구현
6. 곡 후보/투표, 연습곡, 셋리스트 화면 구현 (후보함/연습 중/셋리스트 3탭)
7. (나중) 웹 푸시 알림 추가
